extends layout
block content
    h1 Enter the name of a pokemon
    form(name="search", action="/pokemon", method="get")
        input(type="text", name="name")
        input(type="submit", value="search")